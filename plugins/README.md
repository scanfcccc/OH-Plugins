# Plugin Entries

Each file in this directory is one reviewed marketplace entry. Prefer YAML for
human edits. Run `npm run build:index` after editing this directory so Hana
clients can read the generated `marketplace.json`.
